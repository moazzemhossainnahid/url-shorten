import AppLayout from '@/layouts/AppLayout';
import { ConfigType } from '@/types';
export const Config: ConfigType = {
  appName: 'URL Shorten',
  appDescription: 'URL Shorten - I created URL Shorten App with React, TailwindCSS, and TypeScript',
  layouts: {
    default: AppLayout,
  },
};
